import styles from "./DropMenu.module.css";

const DropMenu = ({ text }) => {
  return (
    <div className={styles.wrapper}>
      <h1 className={styles.title}>Ленд Ровер</h1>
    </div>
  );
};

export default DropMenu;
